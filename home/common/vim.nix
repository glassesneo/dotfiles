{
  # Vim as secondary editor configuration
  # Keep nvim as primary EDITOR, but make vim available
  home.sessionVariables = {
    # You can optionally set VIM_EDITOR or keep vim as secondary
    # EDITOR remains "nvim" from nixvim.nix
  };

  # Optional: Create vim-specific aliases
}
