---
name: REPLACE-WITH-KEBAB-CASE-NAME
description: "REPLACE: First sentence = what this skill does. Following sentences = trigger utterance examples, with searchable terms placed early. Final part = non-use cases and boundaries with neighboring skills. Do not use empty superlatives."
---

# REPLACE-WITH-TITLE

REPLACE: State this skill's responsibility in one sentence.

## Scope / Out of Scope

Scope:

* REPLACE

Out of scope:

* REPLACE: Work delegated to neighboring skills, and work this skill must not perform.

## Input and Output Contract

Input:

* REPLACE

Output:

* REPLACE: If files are generated, specify file names, locations, formats, and update conditions.

Completion condition: REPLACE: Use observable criteria. Do not use subjective criteria such as "when the user is satisfied".

## Procedure

### Step 1: REPLACE

REPLACE: Write one imperative action.

* If REPLACE: REPLACE
* Else: REPLACE

### Step 2: REPLACE

REPLACE

## Exception Handling

REPLACE: Put abnormal cases not handled by the normal procedure here.

## Safety Boundaries

* Read operations: REPLACE
* Write or state-changing operations: REPLACE. If none, explicitly write "No destructive operations".
* Conditions requiring confirmation: REPLACE
* Handling of secrets and personal information: REPLACE

## Dependencies and Environment

* Required CLI/MCP/API: REPLACE
* Network access: REPLACE
* OS differences: REPLACE

## Evaluation Tasks

Following the format in `references/eval-template.md` from `skill-architect`, write at least one positive trigger test, one negative trigger test, and one execution test here or under `references/`.

## Limits

REPLACE: State what this skill cannot do and what inputs it does not assume.

