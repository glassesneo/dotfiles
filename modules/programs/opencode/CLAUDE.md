# modules/programs/opencode/

## Agent Source of Truth

- `prompts/*.md` contains the canonical prompt content for all OpenCode agents in this module.
- `default.nix` owns agent metadata, permissions, shared contract fragments, and placeholder rendering.

## SketchyBar Integration

This module does not deploy a SketchyBar plugin.
