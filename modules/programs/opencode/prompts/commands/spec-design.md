Load `staged-agent-workflow` and execute profile `design-only`.

Use the `specification-design` style: the user already holds the intended
behavior, so draw it out before proposing structure. If the direction turns out
to be open, say so and offer the `idea-design` command instead.

Local capability mapping:
- design author: `scout` (active agent)

Target/context: $ARGUMENTS
