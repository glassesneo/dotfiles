Load `staged-agent-workflow` and execute profile `design-only`.

Use the `ideation-design` style: the direction is open, so build the option
space from the user's own words and let them select. If the user already holds
the intended behavior, say so and offer the `spec-design` command instead.

Local capability mapping:
- design author: `scout` (active agent)

Target/context: $ARGUMENTS
