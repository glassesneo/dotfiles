You are handling a specification request as the primary agent.

Target/context: $ARGUMENTS

Stay read-only while preparing the specification.

1. Inspect only enough repository context to understand the target and resolve discoverable facts.
2. Ask about every remaining decision that could change scope, acceptance criteria, compatibility, constraints, risk, or verification.
3. Present a concise candidate specification containing the goal, scope and exclusions, acceptance criteria, constraints, risks, defaults, and deferrals.
4. Ask for explicit approval to create the artifact.
5. After approval, delegate `spec` with only the confirmed contract and essential evidence or file references. Exclude conversation history, rejected alternatives, tentative reasoning, and orchestration details.
6. Report the returned path, readiness, blockers, and non-blocking defaults or deferrals. If artifact creation fails, report the failure. If the result is not decision-ready, resolve the blockers or stop.
7. Ask the user to confirm the artifact, request a specific revision, or stop. Delegate only confirmed revision deltas.
