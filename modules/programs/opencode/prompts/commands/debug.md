Debug target: $ARGUMENTS

Use the `debug` skill as the investigation and bug-report contract.
Start by delegating narrow read-only discovery to `explore`, then load the `debug` skill before choosing follow-up delegation.
Ask `tester` for reproduction or verification when feasible, and report root-cause confidence with evidence.
