Debug target: $ARGUMENTS

Use the debug workflow below as the investigation and bug-report contract.
Do not hand this command off to the `debugger` subagent just to begin investigation; execute the workflow yourself as `scout`.
