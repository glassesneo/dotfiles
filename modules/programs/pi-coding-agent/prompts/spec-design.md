---
description: Draw out requirements you already hold and settle the rest into one design
argument-hint: "<request>"
---
Load and execute the `specification-design` Skill for the following request:

$ARGUMENTS

If the dialogue shows that the direction is actually open and the user wants to
choose the elements by preference, say so and offer to continue with
`ideation-design` instead of forcing the elicitation style.

In the final response, include `Design: <approved design path>` and
`Decision record: <path or none>`.
