---
description: Create or revise a decision-ready specification
argument-hint: "<request>"
---
Load and execute the `specification-authoring` Skill for the following request:

$ARGUMENTS

After approval and persistence, include the approved specification path in the
final response.
