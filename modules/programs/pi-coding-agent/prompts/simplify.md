---
description: Clean up changed code without changing intended behavior
argument-hint: "[target/context]"
---
Use simplify-workflow for this target. An empty argument uses the Skill's
worktree-default scope.

Target/context: $ARGUMENTS

This entrypoint requires source-mutation authority and ops mode. In recon,
return Outcome: blocked and ask the requester to run /mode ops before retrying.
Keep the current mode unchanged.

For a non-empty scope, apply this Pi adapter to the Skill's four-perspective
protocol. Dispatch four separate concurrent small/read tasks: reuse,
simplification, efficiency, and altitude. Give each the same scope dossier and
source-state marker, its single lens contract, the Skill's Markdown result
contract, and a local stop condition. Each task returns evidence without source
edits, cross-lens consolidation, or overall finding disposition.

Track the four lens-to-task mappings and collect all four results using the
mesh tools' continuation and retrieval contracts. Keep source and overall
finding disposition unchanged until the complete evidence set is available.
Then use the Skill's source-state recheck, joint disposition, cleanup,
verification, and final handoff. Its stale-scope rule requires all four reviews
again for the affected current scope before adopting findings.

Request an additional small/read validation or diagnosis task only when its
independent result can materially improve the decision. For an empty scope,
submit no review tasks and return Outcome: no-op.
